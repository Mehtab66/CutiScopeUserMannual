# Cutiscope User Manual (web)

Single full-screen view of the manual PDF.

- Put the file at `public/cutiscope-user-manual.pdf`, or set `VITE_MANUAL_PDF_URL` to a full PDF URL in `.env`.
- Run `npm run dev` to preview, `npm run build` for production.
